#include <stdlib.h> // getenv
#include <stdint.h>
#include <dlfcn.h>

#include <vector>

using namespace std;

std::vector<char *> get_libraries(std::string libraries_path) {
  if(fs::)
}

__attribute__((constructor)) static void ctor() {
  char *libraries = getenv("LoadLibraries");
}
